spring:
  main:
    allow-bean-definition-overriding: false 
    banner-mode: 'off'
  rest:
    client:
      services:
        localhost: http://localhost
        FooRetryClient: http://localhost
      retry:
        max-attempts: 2

localhost:
  uri: http://localhost
  